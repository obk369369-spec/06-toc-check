# WIC34 MASTER WORKSPACE RULE

## 최종 목적
WIC34의 모든 도구는 화면만 있는 껍데기가 아니라 실제 실행 가능한 완성 도구로 만든다.

## 절대 작업 순서
1. 현재 폴더와 실행 구조를 먼저 조사한다.
2. 기존 파일과 중복 구조를 목록화한다.
3. 실제 사용 중인 파일과 삭제 대상을 구분한다.
4. 삭제 승인 범위 안에서 예전 구조를 먼저 제거한다.
5. 본체 엔진을 먼저 연결한다.
6. 본체가 실제 상태파일을 생성하게 한다.
7. Observer는 상태파일만 읽는다.
8. 타워는 Observer 결과만 표시한다.
9. 실제 입력으로 본체를 1회 실행한다.
10. 결과와 상태 변화가 실제로 일치할 때만 PASS 처리한다.

## 절대 금지
- 화면부터 만들기 금지
- 실제 엔진 없이 진행률 표시 금지
- 실제 로그 없이 PASS/HOLD 표시 금지
- 기존 구조 위에 덧대기 금지
- 같은 역할의 파일 중복 생성 금지
- old, backup, copy, temp, active 구조를 운영 구조로 사용 금지
- I:\, D:\, C:\ 사용자 경로 하드코딩 금지
- 검증 없이 완료 주장 금지
- 오류가 발생할 때마다 새 파일을 추가하는 방식 금지
- 타워가 본체 작업을 대신하게 만들기 금지

## 단일 실행 원칙
사용자가 클릭하는 파일은 루트의 아래 파일 하나다.

WIC34_공통타워_자동찾기.vbs

내부 파일은 CONTROL_TOWER 폴더에만 둔다.

## 역할 분리
본체 도구:
- 실제 업무 처리
- tool_state.json 생성
- 실제 진행률과 단계 기록

Observer:
- 본체 상태 읽기
- snapshot 생성
- 상태 이상 감지

타워:
- snapshot 표시만 수행
- 상태를 직접 생성하지 않음
- 고정 진행률과 고정 설명 사용 금지

## 검은 창 규칙
- cmd.exe 직접 실행 금지
- powershell.exe 가시 실행 금지
- 콘솔형 예약작업 금지
- 사용자 실행은 wscript.exe 또는 비콘솔 실행 구조만 허용
- 검은 창이 한 번이라도 보이면 BLACK_WINDOW_BLOCK=FAIL

## 폴더 정리 규칙
각 개발 작업은 기능 구현과 폴더 정리를 동시에 수행한다.

작업 완료 시:
- 사용자 실행 파일 1개
- 내부 실행 파일 역할 명확
- 중복 HTML 없음
- 중복 VBS 없음
- 중복 Observer 없음
- 불필요한 백업과 임시파일 없음
- 삭제 대상 목록과 유지 대상 목록 기록

## 외부구조 협업
가능한 경우 아래 구조와 실제로 협업한다.

- Antigravity Agent
- Antigravity Terminal
- 실제 프로젝트 폴더
- 실제 본체 엔진
- 실제 로그
- Job Queue
- Playwright
- Screenshot Evidence
- Compare Engine
- GitHub
- Deployment

연결되지 않은 구조는 연결된 것처럼 표시하지 않는다.
연결되지 않았으면 HOLD와 이유를 기록한다.

## 무한 반복 차단
같은 오류가 두 번 반복되면 새 패치를 추가하지 않는다.

반드시:
1. 작업 중단
2. 현재 구조 전체 조사
3. 중복 파일과 충돌 원인 확인
4. 실패 원인 기록
5. 기존 실패 구조 제거
6. 통합 수정 1회
7. 전체 검증 1회

동일 작업을 세 번째 반복하지 않는다.

## PASS 조건
아래 조건을 모두 만족해야 PASS다.

- 본체 실제 실행 성공
- 실제 결과 파일 생성
- tool_state.json을 본체가 직접 생성
- Observer가 상태를 읽음
- 타워가 같은 상태를 표시
- 현재 단계와 쉬운 설명이 실제 상태와 일치
- 중복 파일 없음
- 하드코딩 없음
- 검은 창 없음
- 사용자 실행 파일 1개
- 실제 입력 1회 검증
- 오류 0개

하나라도 부족하면 HOLD.
