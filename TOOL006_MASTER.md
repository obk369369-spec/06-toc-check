# TOOL006 MASTER — 6번 목차 정리 도구

상태: ACTIVE / TOOL-SPECIFIC SINGLE SOURCE
현재 개발상태: HOLD_FOR_REDESIGN / LIMITED_REAL_USE
기준일: 2026-08-30

이 문서는 6번 도구 고유 운영규칙을 보관한다. 현재 실제 상태는 `TOOL006_CHECKPOINT.json`과 `TOOL006_RECOVERY.md`를 함께 읽고 판단한다. WIC 공통 운영규칙은 중앙 글로벌 원본을 우선하며 공통 규칙을 중복 복제하지 않는다.

## 1. 목적
- 왼쪽에 목차 원문을 넣으면 오른쪽에 정리된 목차 결과와 진단 로그를 출력한다.
- 번호/제목/계층을 정리하고 불필요한 TABLE/FIGURE 계열을 제거한다.
- 발행사마다 서로 다른 목차 형식을 다루되, 자동화가 안정적으로 판단할 수 없는 구간은 억지로 자동 보정하지 않는다.

## 2. 사용자 정상 흐름
- 정상 사용은 `자료 입력 → 실행 1번 → 결과`를 목표로 한다.
- 오류가 있는 경우 추가 사용자 동작은 최대 1회 수준의 `자가분석·개선/진단`으로 제한한다.
- 그래도 해결되지 않으면 같은 오류를 반복 수정하지 않고 HOLD/수동 보정 대상으로 넘긴다.
- 사용자는 반복 golden pair 작성, 같은 테스트 재입력, 장시간 오류 설명을 담당하지 않는다.

## 3. 최소 UI/흐름 잠금
- 기본 흐름은 RAW 입력 / 정리 결과 / 로그를 중심으로 유지한다.
- 핵심 동작이 안정되기 전에 버튼·스타일·보조 UI를 계속 확장하지 않는다.
- 기능은 한 번에 하나의 변경 범위만 추가한다.
- 기존 PASS 기능이 깨지면 새 변경은 실패 처리하고 안정판으로 복귀한다.

## 4. 목차 규칙
- TABLE / FIGURE / LIST OF TABLES / LIST OF FIGURES 등 본문 목차가 아닌 목록은 제거 대상이다.
- 상위/하위 번호 구조를 유지한다.
- 하위목차는 계층에 맞게 들여쓰기한다.
- 번호 없는 줄은 문맥/인접 번호/형식을 근거로 후보를 만들 수 있으나 근거가 약하면 자동 확정하지 않는다.
- 동일 입력은 동일 출력이 되어야 한다.
- 발행사/출처별 예외가 반복되면 예외 규칙 후보로 누적하되 한 사례만으로 전체 규칙을 왜곡하지 않는다.

## 5. 자동/수동 혼합
- 검증된 규칙만 자동 처리로 잠근다.
- 자동 실패 시 `MANUAL_REQUIRED`와 원인/ERROR_TAG를 남긴다.
- 구조 차이가 크거나 미확인 패턴은 수동 슬롯으로 전환한다.
- 같은 ERROR_TAG가 실제 다른 입력에서도 반복될 때만 발행사/출처별 예외 규칙 승격 후보로 본다.
- 완전 자동화를 위해 불확실한 변환을 강행하는 것보다 부분 자동 + 수동 보정 + 로그를 우선한다.

## 6. 오류 위치/진단
- 주요 UI 요소는 area_id와 한글 영역명을 통해 오류 위치를 식별할 수 있어야 한다.
- 오류 근거는 최소 `ERROR_POINT / FULL_CODE 또는 오류코드 / AREA_ID / LINE 또는 RAW_SNIPPET / observed / expected / last_action`을 남긴다.
- AUTO_LOG는 화면/행동/관측값 중심, CORE_LOG는 적용 규칙/분기/수동필요/NEXT_FIX 중심으로 분리할 수 있다.
- 표시 로그와 내부 판단이 모순되거나 로그가 비면 PASS 금지.

## 7. 반복 차단
- 같은 코드·같은 입력·같은 환경에서 같은 오류를 반복 재시험하지 않는다.
- 기존 PASS/VERIFIED/REMOTE_VERIFIED 항목은 SKIP_REUSE한다.
- 새 변경 범위에 대해서만 FIRST_VALIDATION 1회 수행한다.
- 동일 실패가 반복되면 미세 패치를 계속 붙이지 않고 구조적 원인/HOLD로 재분류한다.
- 예전 껍데기 작업물을 억지로 연결하지 않는다. 재사용은 실제 PASS 컴포넌트만 허용한다.

## 8. 현재 검증된 재사용 영역
- SELF_ANALYZE, ERROR_CLASSIFY, HISTORICAL_REGRESSION, PASS_ONLY_PROMOTION 등 이미 검증된 범위는 재검증하지 않는다.
- 과거 smoke/functional E2E에서 PASS된 변하지 않은 영역은 그대로 재사용한다.
- 현재 정상 범위는 제한적 실제 사용으로 유지하고, 복잡·다양한 목차까지 전체 자동화가 완료됐다고 주장하지 않는다.

## 9. 현재 HOLD root
- 최근 자가복구 transaction 검증에서 `원본 실행을 끝까지 고정하는 immutable original_run_id` 계약이 부족한 것이 구조적 원인으로 확인된 상태다.
- 동일 문제를 또 패치하지 않는다.
- 재개점은 `immutable original_run_id / original transaction identity` 구조가 검증된 재사용 컴포넌트로 생기거나 사용자가 6번을 명시적으로 우선순위로 올렸을 때다.
- 그 전에는 현재 canonical 안정판을 보존한다.

## 10. 2026-08-30 대화기록 catch-up
- 제공된 6번 과거 기록에서 `새 최소 틀`, 자동/수동 전환, 오류위치 분류, 반복 차단, 안정판 보존의 지속 규칙을 현재 상태와 충돌하지 않게 통합했다.
- 과거에 `처음부터 다시 시작`을 반복하던 흐름은 현재 운영원칙으로 사용하지 않는다. 새 구조가 필요해도 이미 PASS된 컴포넌트는 증거가 있는 경우 재사용한다.
- 이후 6번 대화창에서 사용자가 `업데이트`라고 입력하면 신규 영구 피드백만 이 MASTER와 대조해 DIFF ONLY 반영한다.
- 같은 범위 충돌은 더 최신의 명시적 사용자 지시를 우선한다.
- 실제 GitHub write/commit/read-back 없이 업데이트 완료라고 보고하지 않는다.
